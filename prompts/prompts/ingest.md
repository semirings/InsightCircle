insight_calc/app/main.py. Refactor the following Dart function in main.dart. The function is an HTTP endpoint that accepts a URL as input. The URL can be: 1. A YouTube video URL. 2. A local file path containing multiple YouTube URLs (one per line). The function should: 1. Detect if the input is a single URL or a file. 2. For each video URL: a. Access the YouTube API and download video metadata. b. Convert the metadata into an AA structure (a custom Dart class). 3. Send the AA structure(s) via POST to http://localhost:5202/ins. 4. Handle errors gracefully (invalid URL, network issues, file not found). 5. Log meaningful information for debugging. 6. Be modular: separate fetching, conversion, and posting logic into helper functions. Refactor for clarity, maintainability, and proper async handling. Optimize for readability and error handling. Provide comments explaining key steps.