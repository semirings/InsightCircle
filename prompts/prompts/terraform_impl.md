I need a set of Terraform config files suitable for running each of my microservices as well as my CaaS docker container and my standalone FaaS python function. These will need global variables. Project ID is one: creator-d4m-2026-1774038056. We might find others as we go along. I will also need a bash script called tfc.sh that sits and the Project root (InsightCircle) and runs the terraform command on each directory below. The script takes parameters -i --init == terraform init -p --plan == terraform plan -a --apply == terraform apply