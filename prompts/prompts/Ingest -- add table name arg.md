insight_calc/app/main.py. 

Refactor the following Dart function in main.dart. 
The function is an HTTP endpoint that accepts a URL as input. A parameter called tableName must be added to the endpoint ingest IngestRequest.  
Also the table must already exist.

